import 'dart:io';
import 'dart:math';

void main(List<String> arguments) {
  List <int> list = List.generate(5, (a) => 1+Random().nextInt(10 - 1));
  for (int f = 0; f < list.length; f++) {
   for (var i = 0; i < list.length-1; i++){
    if (num[i] == )
    list.indexOf(i);
    //  var c = 0;
    //  if (list[i] > list[i + 1] ){
    //    c = list[i];
    //    list[i] = list[i + 1];
    //    list[i + 1] = c;
    //  }
   } 
   print(list );
 }
}
